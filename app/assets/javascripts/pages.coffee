$ ->
  # ==========================================================================
  #
  #  ++ pages ++
  #
  # ==========================================================================

  # --------------------------------
  #                     + lazy load +
  # --------------------------------
  $('img.lazy').lazyload({
    threshold: 100, # 200pxの距離まで近づいたら表示する
    effect: "fadeIn",
    effect_speed: "slow"
  })